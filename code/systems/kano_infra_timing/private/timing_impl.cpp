/* timing_impl.cpp — kano_infra_timing private implementation */

#include "kano_timing.h"
/* All timing logic is header-only; this .cpp exists only to satisfy
 * the CMake add_library() convention. No additional implementation needed. */
